[project]
name = "its-contracts"
version = "0.1.0"
description = "ITS contracts - shared JSON schemas and generated type packages."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/its_contracts"]
