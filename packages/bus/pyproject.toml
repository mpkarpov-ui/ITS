[project]
name = "its-bus"
version = "0.1.0"
description = "ITS bus - NATS lifecycle, KV helpers, schema validator."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/its_bus"]
